<%= form_for(@restaurant) do |f| %>
  <%= f.label :name %>:
  <%= f.text_field :name %> </br>

  <%= f.label :address %>:
  <%= f.text_field :address %> </br>

  <%= f.label :capacity %>:
  <%= f.number_field :capacity %> </br>

  <%= f.submit %>

<% end %>
